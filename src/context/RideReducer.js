const RideReducer = (state, action) => {
  switch (action.type) {
    // case "TOGGLE_FILTER":
    //   return {
    //     ...state,
    //     filterBox: false,
    //   };
    default:
      return state;
  }
};

export default RideReducer;
